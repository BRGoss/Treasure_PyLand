<%= simple_form_for(@puzzle) do |f| %>

	<%= f.input :name %>
	<%= f.input :description %>
	<%= f.input :contract %>
	<%= f.input :solution %>

	<%= f.button :submit, class: "btn-primary" %>
<% end %>