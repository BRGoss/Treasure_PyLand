<%= simple_form_for(@scene) do |f| %>
	
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :transcript %>
	<%= f.input :visual %>

	<%= f.button :submit, class: "btn-primary" %>
<% end %>