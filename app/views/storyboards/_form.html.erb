<%= simple_form_for(@storyboard) do |f| %>
	
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :default, label: "Use As Default" %>
	<%= f.input :debug_mode, label: "Set Debug Mode On" %>

	<%= f.button :submit, class: "btn-primary" %>

<% end %>